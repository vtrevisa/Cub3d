{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch with LLDB",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/cub3D",
            "args": ["map_simple_R.cub"],
            "preLaunchTask": "clang build",
            "cwd": "${workspaceFolder}"
        }
    ]
}